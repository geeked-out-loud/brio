// Valid Braille keys for FEWJIO mapping
export const VALID_BRAILLE_KEYS = ['f', 'e', 'w', 'j', 'i', 'o'] as const;

// Special keys
export const SPECIAL_KEYS = {
  BACKSPACE: 'Backspace',
  SPACE: ' ',
} as const;
